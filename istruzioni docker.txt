i dockerfile sono nelle cartelle

fatti i file vanno buildati le "images" che sarebbero le vm: docker build -t [your_flask_image_name] -f [path_to_flask_dockerfile] 
fatto ciò si inseriscono i nomi delle images nel docker-compose e si runna tutto dal docker-compose con un solo comando

runna il docker compose con docker-compose up backend frontend db (in questo modo non runna l'installer del db)
runna poi a parte l'installer con docker-compose run install-db (solo 1 volta)